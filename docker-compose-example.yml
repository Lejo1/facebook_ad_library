# Example file showing all services you might want to run
version: '3'

services:
  api_crawler:
    build: ./crawler
    restart: always
    environment:
      PYTHONUNBUFFERED: 1
    stop_signal: SIGINT
    env_file:
      - secrets.env

  web:
    build: ./web
    restart: always
    environment:
      - GIN_MODE=release
      - DB_URL=mongodb://...
      - B2_APPLICATION_KEY_ID=
      - B2_APPLICATION_KEY=
      - B2_BUCKET_NAME=
      - JWT_SECRET=
    ports:
      - 8080:8080
